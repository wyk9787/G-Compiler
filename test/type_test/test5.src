(let
  [fact : int->float] = rec f [n:int] : int ->
  if (n > 0) then
    n * f (n-1)
  else
    1
in fact (5))

+

(let [max2 : int-> int -> int] =
  fun [x:int] : [int->int] -> (fun [y:int] : int -> if x > y then x else y)
in
let [a:int] = 5 in
let [b:float] = 2.0 in
max2 (a) (b))
