#include <language/lib/math.glib>

let init(a : unit) : unit = 
  [-] a = {
    struct {
      int x => 3,
      int y => 4,
    }
  };

  [-] b = {
    struct {
      int x => a.y,
      int y => a.x,
    }
  }
;;

let main(a : int) : int =
  (max2 b.x b.y) + b.x
;;
